%table.table.table-condensed
  %thead
    %tr
      %th.col-sm-1 ID
      %th.col-sm-11 Name
  %tbody
    - @disciplines.each do |d|
      %tr
        %td= d.id
        %td= link_to d.name, edit_discipline_path(id: d.id)
= link_to("Add", new_discipline_path , class: "btn btn-default active")
    